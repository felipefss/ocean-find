import { describe, expect, it } from "vitest";

// Placeholder — real tests are added in Phase 3 (scrapers + parsers)
describe("scraper bootstrap", () => {
  it("should be true", () => {
    expect(true).toBe(true);
  });
});
